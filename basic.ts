// Premitive Types
// string, number, boolean, enum, any, void, null, undefined

const message: string = "12";
const age: number = 12;
const isPublished: boolean = true;

const numbers: number[] = [1, 2, 3];
const names: string[] = ["Rahim", "Karim"];

const flags: boolean[] = [true, false];

// enum Role {
//   ADMIN,
//   MANAGER,
//   USER,
//   GUEST,
// }

enum Status {
  PUBLISHED,
  DRAFT,
  DELETED,
}
